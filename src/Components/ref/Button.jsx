import React from 'react'

function Button(props) {
  return (
    <div>
        {/* <button onClick={() => {props.inputRefUserName.current.focus()}}>Set Focus</button> */}
    </div>
  )
}

export default Button